'use client'
import { useState } from 'react'; import Section from '../../../components/Section'
export default function AdminLogin(){ const [password,setPassword]=useState(''); const [msg,setMsg]=useState(null); async function onSubmit(e){ e.preventDefault(); const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body: JSON.stringify({ password })}); const d=await r.json(); if(!r.ok){ setMsg('Error: '+d.error); return } window.location.href='/admin' } return (<Section title="Admin Login"><form onSubmit={onSubmit} className="grid gap-3 max-w-sm"><input type="password" className="input" placeholder="ADMIN_SECRET" value={password} onChange={e=>setPassword(e.target.value)} /><button className="btn">Login</button>{msg && <p className="text-sm">{msg}</p>}</form></Section>) }
